import { SubjectListSortDirective } from './subject-list-sort.directive';

describe('SubjectListDirective', () => {
  it('should create an instance', () => {
    const directive = new SubjectListSortDirective();
    expect(directive).toBeTruthy();
  });
});
